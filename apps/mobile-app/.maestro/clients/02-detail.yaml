appId: com.handysuites.app
name: "Clientes - Detalle"
---

# Login VENDEDOR via helper (la sesion NO persiste en cold-start: el runner
# relanza a la pantalla de login). Deja el dashboard "Hoy" cargado.
- runFlow:
    file: ../_helpers/login-as.yaml
    env:
      EMAIL: "vendedor1@jeyma.com"
      PASSWORD: "test123"

# Tap en el primer cliente de la lista
- tapOn:
    index: 0
    childOf:
      enabled: true

- extendedWaitUntil:
    visible: "Tel"
    timeout: 15000
    optional: true

- takeScreenshot: client-detail

# Regresar
- back
